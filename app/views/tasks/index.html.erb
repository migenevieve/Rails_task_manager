<h1>Index</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task.id)%> <%= link_to "delete", destroy_task_path(task), method: :delete %></li>

  <% end %>

</ul>

<%= link_to "+ Add a new task", "/tasks/new" %>
